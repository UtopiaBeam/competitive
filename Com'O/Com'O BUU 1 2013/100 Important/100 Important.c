#include<stdio.h>

int main()
{
    double x0=0,y0=0,x=0,y=0;
    scanf("%lf %lf",&x0,&y0);
    if(x0>=0 && x0<250 && y0==0)
    {
        x=500.0-(125000.0/(500.0-x0));
        y=125000.0/(500.0-x0);
    }
    else if(x0>=250 && x0<=500 && y0==0)
    {
        x=0.0;
        y=125000.0/x0;
    }
    else if(y0>=0 && y0<250 && x0==0)
    {
        x=125000.0/(500.0-y0);
        y=500.0-(125000.0/(500.0-y0));
    }
    else if(y0>=250 && y0<=500 && x0==0)
    {
        x=0.0;
        y=125000.0/y0;
    }
    else if(x0>0 && x0<=250 && y0>=250 && y0<500)
    {
        x=500.0-(125000.0/y0);
        y=0.0;
    }
    else if(x0>250 && x0<=500 && y0>0 && y0<=250)
    {
        x=0.0;
        y=500.0-(125000.0/x0);
    }

    printf("%.2lf %.2lf",x,y);
    return 0;
}
