#include <cstdio>
int n, k, a[1002];
bool dp[10000002] = {true};
int main() {
    scanf("%d %d", &n, &k);
    for(int i = 0; i < n; i++)
        scanf("%d", a+i);
    while(k--) {
        for(int i = 0; i < n; i++) {
            ;
        }
    }
}
